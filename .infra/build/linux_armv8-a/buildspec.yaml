version: 0.2

env:
  variables:
    BINARY_NAME: netsurf-linux_armv8-a
    CGO_ENABLED: 1
    GOOS: linux
    GOARCH: arm64
    CFLAGS: -march=armv8-a

phases:
  build:
    commands:
      - #!/bin/sh
      - task build/base

artifacts:
  files:
    - ./bin/${BINARY_NAME}